[pytest]
markers =
    performance: marks GPU throughput or other performance-sensitive tests
    gpu: marks tests that require GPU availability
testpaths =
    tests
    training/tests
    training/datasets/tests
